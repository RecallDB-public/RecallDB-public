# SEO Optimisation

Implemented for the public RecallDB repository:

- Dataset, FAQ and page-level OpenGraph metadata on the landing page.
- Agency-specific SEO pages under `agencies/`.
- Hazard taxonomy SEO pages under `hazards/`.
- `sitemap.xml`, `robots.txt`, canonical URLs and social preview image.
- Fast static HTML/CSS/JS with no external runtime dependencies.
